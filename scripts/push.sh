git add 2020/monthly/01/*.gz
git ci -m "jan 15m non-margin pairs"
git push

git add 2020/monthly/02/*.gz
git ci -m "feb 15m non-margin pairs"
git push

git add 2020/monthly/03/*.gz
git ci -m "mar 15m non-margin pairs"
git push

git add 2020/monthly/04/*.gz
git ci -m "apr 15m non-margin pairs"
git push

git add 2020/monthly/05/*.gz
git ci -m "may 15m non-margin pairs"
git push

git add 2020/monthly/06/*.gz
git ci -m "jun 15m non-margin pairs"
git push

git add 2020/monthly/07/*.gz
git ci -m "jul 15m non-margin pairs"
git push

git add 2020/monthly/08/*.gz
git ci -m "aug 15m non-margin pairs"
git push

git add 2020/monthly/09/*.gz
git ci -m "sep 15m non-margin pairs"
git push

git add 2020/monthly/10/*.gz
git ci -m "oct 15m non-margin pairs"
git push

git add 2020/monthly/11/*.gz
git ci -m "nov 15m non-margin pairs"
git push

git add 2020/monthly/12/*.gz
git ci -m "dec 15m non-margin pairs"
git push

